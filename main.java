import java.util.Scanner;
import Lib.index;
public class main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        index.menu(scanner);
        scanner.close();
    }
}
