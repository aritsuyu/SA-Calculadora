import java.util.Scanner;
import Lib.index;

public class code {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        index.menu(scanner);
        scanner.close();
    }
}
